local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local NightUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/asutse0/NightUILibrary/refs/heads/main/Project"))()

local Window = NightUI:CreateWindow({
	Title = "Flee the Facility",
	KeySystem = true,
	Key = "Flee22",
	DiscordLink = "https://discord.gg/Jc5dpWqBtZ"
})

local Visuals = Window:CreateTab("Visuals")
local Movement = Window:CreateTab("Movement")

local highlightSurvivorsEnabled = false
local highlightBeastEnabled = false
local highlightComputersEnabled = false
local infiniteWallHopEnabled = false
local ghostModeEnabled = false

local highlightFolder = PlayerGui:FindFirstChild("DebugHighlights")
if not highlightFolder then
	highlightFolder = Instance.new("Folder")
	highlightFolder.Name = "DebugHighlights"
	highlightFolder.Parent = PlayerGui
end

local characterConnections = {}

--// PLAYER HIGHLIGHT LOGIC

local function hasHammer(character)
	if not character then
		return false
	end

	local hammer = character:FindFirstChild("Hammer")
	return hammer ~= nil and hammer:IsA("Model")
end

local function getPlayerHighlightName(player, highlightType)
	return player.Name .. "_" .. highlightType .. "_Highlight"
end

local function removePlayerHighlight(player, highlightType)
	local existing = highlightFolder:FindFirstChild(getPlayerHighlightName(player, highlightType))

	if existing then
		existing:Destroy()
	end
end

local function createOrUpdatePlayerHighlight(player, character, highlightType, outlineColor)
	local highlightName = getPlayerHighlightName(player, highlightType)
	local existing = highlightFolder:FindFirstChild(highlightName)

	if existing then
		existing.Adornee = character
		existing.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
		existing.FillTransparency = 1
		existing.OutlineTransparency = 0
		existing.OutlineColor = outlineColor
		return
	end

	local highlight = Instance.new("Highlight")
	highlight.Name = highlightName
	highlight.Adornee = character
	highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
	highlight.FillTransparency = 1
	highlight.OutlineTransparency = 0
	highlight.OutlineColor = outlineColor
	highlight.Parent = highlightFolder
end

local function updatePlayerHighlights()
	for _, player in ipairs(Players:GetPlayers()) do
		if player ~= LocalPlayer then
			local character = player.Character

			if character then
				local beast = hasHammer(character)
				local survivor = not beast

				if highlightSurvivorsEnabled and survivor then
					createOrUpdatePlayerHighlight(
						player,
						character,
						"Survivor",
						Color3.fromRGB(0, 255, 100)
					)
				else
					removePlayerHighlight(player, "Survivor")
				end

				if highlightBeastEnabled and beast then
					createOrUpdatePlayerHighlight(
						player,
						character,
						"Beast",
						Color3.fromRGB(255, 60, 60)
					)
				else
					removePlayerHighlight(player, "Beast")
				end
			else
				removePlayerHighlight(player, "Survivor")
				removePlayerHighlight(player, "Beast")
			end
		end
	end
end

local function clearSurvivorHighlights()
	for _, player in ipairs(Players:GetPlayers()) do
		removePlayerHighlight(player, "Survivor")
	end
end

local function clearBeastHighlights()
	for _, player in ipairs(Players:GetPlayers()) do
		removePlayerHighlight(player, "Beast")
	end
end

local function watchCharacter(player, character)
	if characterConnections[player] then
		for _, connection in ipairs(characterConnections[player]) do
			connection:Disconnect()
		end
	end

	characterConnections[player] = {}

	table.insert(characterConnections[player], character.ChildAdded:Connect(function(child)
		if child.Name == "Hammer" then
			updatePlayerHighlights()
		end
	end))

	table.insert(characterConnections[player], character.ChildRemoved:Connect(function(child)
		if child.Name == "Hammer" then
			updatePlayerHighlights()
		end
	end))

	updatePlayerHighlights()
end

local function watchPlayer(player)
	if player == LocalPlayer then
		return
	end

	if player.Character then
		watchCharacter(player, player.Character)
	end

	player.CharacterAdded:Connect(function(character)
		task.wait(0.25)
		watchCharacter(player, character)
	end)

	player.CharacterRemoving:Connect(function()
		removePlayerHighlight(player, "Survivor")
		removePlayerHighlight(player, "Beast")
	end)
end

--// COMPUTER HIGHLIGHT LOGIC

local computerHighlights = {}
local computerColorConnections = {}
local computerRefreshConnection = nil
local computerDescendantAddedConnection = nil
local computerDescendantRemovingConnection = nil

local DEFAULT_COMPUTER_COLOR = Color3.fromRGB(0, 200, 255)

local function isComputerTable(model)
	return model ~= nil and model.Name == "ComputerTable" and model:IsA("Model")
end

local function findDeep(parent, name)
	if not parent then
		return nil
	end

	return parent:FindFirstChild(name, true)
end

local function getScreenPart(computerTable)
	if not computerTable then
		return nil
	end

	local screen = findDeep(computerTable, "Screen")

	if not screen then
		return nil
	end

	if screen:IsA("BasePart") then
		return screen
	end

	if screen:IsA("Model") or screen:IsA("Folder") then
		for _, descendant in ipairs(screen:GetDescendants()) do
			if descendant:IsA("BasePart") then
				return descendant
			end
		end
	end

	return nil
end

local function getComputerColor(computerTable)
	local screenPart = getScreenPart(computerTable)

	if screenPart then
		return screenPart.Color
	end

	return DEFAULT_COMPUTER_COLOR
end

local function disconnectComputerColorConnections(computerTable)
	if computerColorConnections[computerTable] then
		for _, connection in ipairs(computerColorConnections[computerTable]) do
			if connection then
				connection:Disconnect()
			end
		end

		computerColorConnections[computerTable] = nil
	end
end

local function removeComputerHighlight(computerTable)
	local highlight = computerHighlights[computerTable]

	if highlight and highlight.Parent then
		highlight:Destroy()
	end

	computerHighlights[computerTable] = nil
	disconnectComputerColorConnections(computerTable)
end

local function clearComputerHighlights()
	for computerTable, highlight in pairs(computerHighlights) do
		if highlight and highlight.Parent then
			highlight:Destroy()
		end
	end

	for computerTable, connections in pairs(computerColorConnections) do
		for _, connection in ipairs(connections) do
			if connection then
				connection:Disconnect()
			end
		end
	end

	computerHighlights = {}
	computerColorConnections = {}

	if computerRefreshConnection then
		computerRefreshConnection:Disconnect()
		computerRefreshConnection = nil
	end

	if computerDescendantAddedConnection then
		computerDescendantAddedConnection:Disconnect()
		computerDescendantAddedConnection = nil
	end

	if computerDescendantRemovingConnection then
		computerDescendantRemovingConnection:Disconnect()
		computerDescendantRemovingConnection = nil
	end
end

local function hookScreenColor(computerTable, highlight)
	disconnectComputerColorConnections(computerTable)

	computerColorConnections[computerTable] = {}

	local screenPart = getScreenPart(computerTable)

	if not screenPart then
		return
	end

	local colorConnection = screenPart:GetPropertyChangedSignal("Color"):Connect(function()
		if highlight and highlight.Parent then
			highlight.OutlineColor = screenPart.Color
		end
	end)

	local brickColorConnection = screenPart:GetPropertyChangedSignal("BrickColor"):Connect(function()
		if highlight and highlight.Parent then
			highlight.OutlineColor = screenPart.BrickColor.Color
		end
	end)

	table.insert(computerColorConnections[computerTable], colorConnection)
	table.insert(computerColorConnections[computerTable], brickColorConnection)
end

local function createOrUpdateComputerHighlight(computerTable)
	if not highlightComputersEnabled then
		return
	end

	if not isComputerTable(computerTable) then
		removeComputerHighlight(computerTable)
		return
	end

	local color = getComputerColor(computerTable)
	local existing = computerHighlights[computerTable]

	if existing and existing.Parent then
		existing.Adornee = computerTable
		existing.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
		existing.FillTransparency = 1
		existing.OutlineTransparency = 0
		existing.OutlineColor = color

		if not computerColorConnections[computerTable] then
			hookScreenColor(computerTable, existing)
		end

		return
	end

	local highlight = Instance.new("Highlight")
	highlight.Name = "ComputerHighlight"
	highlight.Adornee = computerTable
	highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
	highlight.FillTransparency = 1
	highlight.OutlineTransparency = 0
	highlight.OutlineColor = color
	highlight.Parent = highlightFolder

	computerHighlights[computerTable] = highlight
	hookScreenColor(computerTable, highlight)
end

local function updateComputerHighlights()
	if not highlightComputersEnabled then
		return
	end

	for _, obj in ipairs(Workspace:GetDescendants()) do
		if isComputerTable(obj) then
			createOrUpdateComputerHighlight(obj)
		end
	end

	for computerTable, highlight in pairs(computerHighlights) do
		if not computerTable or not computerTable.Parent or not isComputerTable(computerTable) then
			removeComputerHighlight(computerTable)
		else
			local color = getComputerColor(computerTable)

			if highlight and highlight.Parent then
				highlight.Adornee = computerTable
				highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
				highlight.FillTransparency = 1
				highlight.OutlineTransparency = 0
				highlight.OutlineColor = color
			else
				removeComputerHighlight(computerTable)
			end
		end
	end
end

local function enableComputerHighlights()
	clearComputerHighlights()

	highlightComputersEnabled = true
	updateComputerHighlights()

	local lastFullRefresh = 0

	computerRefreshConnection = RunService.Heartbeat:Connect(function()
		if not highlightComputersEnabled then
			return
		end

		local now = os.clock()

		if now - lastFullRefresh >= 0.5 then
			lastFullRefresh = now
			updateComputerHighlights()
		end
	end)

	computerDescendantAddedConnection = Workspace.DescendantAdded:Connect(function(descendant)
		if not highlightComputersEnabled then
			return
		end

		if descendant.Name == "ComputerTable" or descendant.Name == "Screen" then
			task.delay(0.2, function()
				if highlightComputersEnabled then
					updateComputerHighlights()
				end
			end)
		end
	end)

	computerDescendantRemovingConnection = Workspace.DescendantRemoving:Connect(function(descendant)
		if not highlightComputersEnabled then
			return
		end

		if descendant.Name == "ComputerTable" then
			removeComputerHighlight(descendant)
			return
		end

		if descendant.Name == "Screen" then
			task.defer(function()
				if highlightComputersEnabled then
					updateComputerHighlights()
				end
			end)
		end
	end)
end

local function disableComputerHighlights()
	highlightComputersEnabled = false
	clearComputerHighlights()
end

--// INFINITE WALL HOP LOGIC
--// This only infinite jumps. No turning.

local wallHopConnection = nil
local lastWallHopJump = 0
local wallHopJumpCooldown = 0.08

local function doInfiniteWallHopJump()
	if not infiniteWallHopEnabled then
		return
	end

	local now = os.clock()
	if now - lastWallHopJump < wallHopJumpCooldown then
		return
	end

	lastWallHopJump = now

	local character = LocalPlayer.Character
	if not character then
		return
	end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then
		return
	end

	humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
end

local function enableInfiniteWallHop()
	infiniteWallHopEnabled = true

	if wallHopConnection then
		wallHopConnection:Disconnect()
		wallHopConnection = nil
	end

	wallHopConnection = UserInputService.JumpRequest:Connect(function()
		doInfiniteWallHopJump()
	end)
end

local function disableInfiniteWallHop()
	infiniteWallHopEnabled = false

	if wallHopConnection then
		wallHopConnection:Disconnect()
		wallHopConnection = nil
	end
end

--// GHOST MODE LOGIC
--// Real character is repeatedly teleported 50 studs below. No anchoring.

local ghostClone = nil
local ghostConnection = nil
local ghostNoclipConnection = nil
local ghostRealKeepConnection = nil
local ghostRealCharacter = nil
local ghostRealRoot = nil
local ghostRealHumanoid = nil
local ghostOriginalCameraSubject = nil
local ghostOriginalCFrame = nil
local ghostSafeCFrame = nil
local ghostFlySpeed = 42

local ghostMobileGui = nil
local ghostMobileUpHeld = false
local ghostMobileDownHeld = false

local ghostSavedPartStates = {}
local ghostSavedHumanoidState = nil

local controls = nil

pcall(function()
	local playerScripts = LocalPlayer:WaitForChild("PlayerScripts")
	local playerModule = require(playerScripts:WaitForChild("PlayerModule"))
	controls = playerModule:GetControls()
end)

local function getCharacterParts(character)
	if not character then
		return nil, nil
	end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	local rootPart = character:FindFirstChild("HumanoidRootPart")

	return humanoid, rootPart
end

local function saveRealCharacterState(character, humanoid)
	ghostSavedPartStates = {}
	ghostSavedHumanoidState = nil

	if character then
		for _, descendant in ipairs(character:GetDescendants()) do
			if descendant:IsA("BasePart") then
				ghostSavedPartStates[descendant] = {
					CanCollide = descendant.CanCollide,
					CanTouch = descendant.CanTouch,
					CanQuery = descendant.CanQuery,
					Anchored = descendant.Anchored,
					Massless = descendant.Massless,
					Transparency = descendant.Transparency
				}
			end
		end
	end

	if humanoid then
		ghostSavedHumanoidState = {
			PlatformStand = humanoid.PlatformStand,
			AutoRotate = humanoid.AutoRotate,
			WalkSpeed = humanoid.WalkSpeed,
			JumpPower = humanoid.JumpPower,
			JumpHeight = humanoid.JumpHeight,
			UseJumpPower = humanoid.UseJumpPower,
			Sit = humanoid.Sit
		}
	end
end

local function restoreRealCharacterState()
	for part, state in pairs(ghostSavedPartStates) do
		if part and part.Parent then
			part.CanCollide = state.CanCollide
			part.CanTouch = state.CanTouch
			part.CanQuery = state.CanQuery
			part.Anchored = state.Anchored
			part.Massless = state.Massless
			part.Transparency = state.Transparency
			part.AssemblyLinearVelocity = Vector3.zero
			part.AssemblyAngularVelocity = Vector3.zero
		end
	end

	if ghostRealHumanoid and ghostRealHumanoid.Parent and ghostSavedHumanoidState then
		ghostRealHumanoid.PlatformStand = ghostSavedHumanoidState.PlatformStand
		ghostRealHumanoid.AutoRotate = ghostSavedHumanoidState.AutoRotate
		ghostRealHumanoid.WalkSpeed = ghostSavedHumanoidState.WalkSpeed
		ghostRealHumanoid.JumpPower = ghostSavedHumanoidState.JumpPower
		ghostRealHumanoid.JumpHeight = ghostSavedHumanoidState.JumpHeight
		ghostRealHumanoid.UseJumpPower = ghostSavedHumanoidState.UseJumpPower
		ghostRealHumanoid.Sit = ghostSavedHumanoidState.Sit
	end
end

local function setArchivableRecursive(instance, value)
	if not instance then
		return
	end

	pcall(function()
		instance.Archivable = value
	end)

	for _, descendant in ipairs(instance:GetDescendants()) do
		pcall(function()
			descendant.Archivable = value
		end)
	end
end

local function setRealCharacterSafeState(character)
	if not character then
		return
	end

	for _, descendant in ipairs(character:GetDescendants()) do
		if descendant:IsA("BasePart") then
			descendant.CanCollide = false
			descendant.CanTouch = false
			descendant.CanQuery = false
			descendant.Anchored = false
			descendant.AssemblyLinearVelocity = Vector3.zero
			descendant.AssemblyAngularVelocity = Vector3.zero
		end
	end
end

local function prepareGhostCharacter(character)
	for _, descendant in ipairs(character:GetDescendants()) do
		if descendant:IsA("BasePart") then
			descendant.CanCollide = false
			descendant.CanTouch = false
			descendant.CanQuery = false
			descendant.Massless = true
			descendant.AssemblyLinearVelocity = Vector3.zero
			descendant.AssemblyAngularVelocity = Vector3.zero

			if descendant.Name == "HumanoidRootPart" then
				descendant.Transparency = 1
			else
				descendant.Transparency = math.clamp(descendant.Transparency + 0.35, 0, 0.85)
			end
		elseif descendant:IsA("Decal") then
			descendant.Transparency = math.clamp(descendant.Transparency + 0.35, 0, 0.85)
		elseif descendant:IsA("Script") or descendant:IsA("LocalScript") then
			descendant:Destroy()
		end
	end
end

local function setGhostNoclip(character)
	if not character then
		return
	end

	for _, descendant in ipairs(character:GetDescendants()) do
		if descendant:IsA("BasePart") then
			descendant.CanCollide = false
			descendant.CanTouch = false
			descendant.CanQuery = false
			descendant.AssemblyLinearVelocity = Vector3.zero
			descendant.AssemblyAngularVelocity = Vector3.zero
		end
	end
end

local function getBelowSafeCFrame(originCFrame)
	return originCFrame * CFrame.new(0, -50, 0)
end

local function keepRealCharacterBelow()
	if not ghostRealCharacter or not ghostRealRoot or not ghostRealRoot.Parent then
		return
	end

	setRealCharacterSafeState(ghostRealCharacter)

	ghostRealRoot.CFrame = ghostSafeCFrame
	ghostRealRoot.AssemblyLinearVelocity = Vector3.zero
	ghostRealRoot.AssemblyAngularVelocity = Vector3.zero

	if ghostRealHumanoid and ghostRealHumanoid.Parent then
		ghostRealHumanoid.PlatformStand = true
		ghostRealHumanoid.AutoRotate = false
		ghostRealHumanoid.Sit = false
	end
end

local function moveRealCharacterToSafeSpot()
	if not ghostRealRoot then
		return
	end

	ghostSafeCFrame = getBelowSafeCFrame(ghostOriginalCFrame)
	keepRealCharacterBelow()
end

local function getControlMoveVector()
	if controls then
		local success, moveVector = pcall(function()
			return controls:GetMoveVector()
		end)

		if success and typeof(moveVector) == "Vector3" then
			return moveVector
		end
	end

	return Vector3.zero
end

local function createGhostMobileButtons()
	if ghostMobileGui then
		ghostMobileGui:Destroy()
		ghostMobileGui = nil
	end

	ghostMobileUpHeld = false
	ghostMobileDownHeld = false

	local gui = Instance.new("ScreenGui")
	gui.Name = "GhostModeMobileControls"
	gui.ResetOnSpawn = false
	gui.IgnoreGuiInset = true
	gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	gui.Parent = PlayerGui

	local function makeButton(name, text, position)
		local button = Instance.new("TextButton")
		button.Name = name
		button.Size = UDim2.fromOffset(72, 72)
		button.Position = position
		button.BackgroundColor3 = Color3.fromRGB(15, 15, 22)
		button.BackgroundTransparency = 0.15
		button.Text = text
		button.TextColor3 = Color3.fromRGB(245, 245, 255)
		button.TextSize = 16
		button.Font = Enum.Font.GothamBold
		button.AutoButtonColor = false
		button.ZIndex = 200
		button.Parent = gui

		local corner = Instance.new("UICorner")
		corner.CornerRadius = UDim.new(0, 999)
		corner.Parent = button

		return button
	end

	local upButton = makeButton("UpButton", "UP", UDim2.new(1, -92, 0.55, -80))
	local downButton = makeButton("DownButton", "DOWN", UDim2.new(1, -92, 0.55, 0))

	local function bindHold(button, setState)
		button.InputBegan:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.Touch
				or input.UserInputType == Enum.UserInputType.MouseButton1 then
				setState(true)
			end
		end)

		button.InputEnded:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.Touch
				or input.UserInputType == Enum.UserInputType.MouseButton1 then
				setState(false)
			end
		end)

		button.MouseLeave:Connect(function()
			setState(false)
		end)
	end

	bindHold(upButton, function(value)
		ghostMobileUpHeld = value
	end)

	bindHold(downButton, function(value)
		ghostMobileDownHeld = value
	end)

	ghostMobileGui = gui
end

local function destroyGhostMobileButtons()
	ghostMobileUpHeld = false
	ghostMobileDownHeld = false

	if ghostMobileGui then
		ghostMobileGui:Destroy()
		ghostMobileGui = nil
	end
end

local function getGhostMoveVector()
	local rawMove = getControlMoveVector()

	local camera = Workspace.CurrentCamera
	local moveVector = Vector3.zero

	if camera then
		local cameraCF = camera.CFrame
		local forward = Vector3.new(cameraCF.LookVector.X, 0, cameraCF.LookVector.Z)
		local right = Vector3.new(cameraCF.RightVector.X, 0, cameraCF.RightVector.Z)

		if forward.Magnitude > 0 then
			forward = forward.Unit
		end

		if right.Magnitude > 0 then
			right = right.Unit
		end

		moveVector = (right * rawMove.X) + (forward * -rawMove.Z)
	else
		moveVector = Vector3.new(rawMove.X, 0, rawMove.Z)
	end

	local vertical = 0

	if UserInputService:IsKeyDown(Enum.KeyCode.Space) or ghostMobileUpHeld then
		vertical += 1
	end

	if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl)
		or UserInputService:IsKeyDown(Enum.KeyCode.LeftShift)
		or ghostMobileDownHeld then
		vertical -= 1
	end

	moveVector = Vector3.new(moveVector.X, vertical, moveVector.Z)

	if moveVector.Magnitude > 1 then
		moveVector = moveVector.Unit
	end

	return moveVector
end

local function restoreRealCharacterToGhostPosition()
	local targetCFrame = ghostOriginalCFrame

	if ghostClone then
		local _, ghostRoot = getCharacterParts(ghostClone)

		if ghostRoot then
			targetCFrame = ghostRoot.CFrame
		end
	end

	if ghostRealRoot and ghostRealRoot.Parent then
		ghostRealRoot.CFrame = targetCFrame
		ghostRealRoot.AssemblyLinearVelocity = Vector3.zero
		ghostRealRoot.AssemblyAngularVelocity = Vector3.zero
	end

	restoreRealCharacterState()

	if ghostRealHumanoid and ghostRealHumanoid.Parent then
		ghostRealHumanoid:ChangeState(Enum.HumanoidStateType.GettingUp)

		task.defer(function()
			if ghostRealHumanoid and ghostRealHumanoid.Parent then
				ghostRealHumanoid:ChangeState(Enum.HumanoidStateType.Running)
			end
		end)
	end

	if Workspace.CurrentCamera and ghostRealHumanoid then
		Workspace.CurrentCamera.CameraSubject = ghostRealHumanoid
	end
end

local function enableGhostMode()
	if ghostModeEnabled then
		return
	end

	local character = LocalPlayer.Character
	if not character then
		return
	end

	local realHumanoid, realRoot = getCharacterParts(character)
	if not realHumanoid or not realRoot then
		return
	end

	ghostModeEnabled = true

	ghostRealCharacter = character
	ghostRealHumanoid = realHumanoid
	ghostRealRoot = realRoot
	ghostOriginalCFrame = realRoot.CFrame
	ghostOriginalCameraSubject = Workspace.CurrentCamera and Workspace.CurrentCamera.CameraSubject or nil

	saveRealCharacterState(character, realHumanoid)
	setArchivableRecursive(character, true)

	local clone = character:Clone()
	if not clone then
		ghostModeEnabled = false
		restoreRealCharacterState()
		return
	end

	clone.Name = LocalPlayer.Name .. "_GhostModeClone"
	clone.Parent = Workspace
	ghostClone = clone

	local ghostHumanoid, ghostRoot = getCharacterParts(clone)

	if not ghostHumanoid or not ghostRoot then
		ghostModeEnabled = false
		restoreRealCharacterState()

		if ghostClone then
			ghostClone:Destroy()
			ghostClone = nil
		end

		return
	end

	prepareGhostCharacter(clone)

	ghostRoot.CFrame = ghostOriginalCFrame
	ghostRoot.Anchored = true
	ghostRoot.AssemblyLinearVelocity = Vector3.zero
	ghostRoot.AssemblyAngularVelocity = Vector3.zero

	ghostHumanoid.AutoRotate = false
	ghostHumanoid.PlatformStand = true
	ghostHumanoid.WalkSpeed = 0
	ghostHumanoid.JumpPower = 0

	moveRealCharacterToSafeSpot()
	createGhostMobileButtons()

	if Workspace.CurrentCamera then
		Workspace.CurrentCamera.CameraSubject = ghostHumanoid
	end

	if ghostNoclipConnection then
		ghostNoclipConnection:Disconnect()
		ghostNoclipConnection = nil
	end

	ghostNoclipConnection = RunService.Stepped:Connect(function()
		if ghostClone then
			setGhostNoclip(ghostClone)
		end
	end)

	if ghostRealKeepConnection then
		ghostRealKeepConnection:Disconnect()
		ghostRealKeepConnection = nil
	end

	ghostRealKeepConnection = RunService.Heartbeat:Connect(function()
		if ghostModeEnabled then
			keepRealCharacterBelow()
		end
	end)

	if ghostConnection then
		ghostConnection:Disconnect()
		ghostConnection = nil
	end

	ghostConnection = RunService.RenderStepped:Connect(function(deltaTime)
		if not ghostModeEnabled then
			return
		end

		if not ghostClone or not ghostClone.Parent then
			return
		end

		local _, currentGhostRoot = getCharacterParts(ghostClone)
		if not currentGhostRoot then
			return
		end

		currentGhostRoot.Anchored = true

		local moveVector = getGhostMoveVector()

		if moveVector.Magnitude > 0 then
			local newPosition = currentGhostRoot.Position + (moveVector * ghostFlySpeed * deltaTime)

			local camera = Workspace.CurrentCamera
			local lookDirection = currentGhostRoot.CFrame.LookVector

			if camera then
				local cameraLook = camera.CFrame.LookVector
				local flatLook = Vector3.new(cameraLook.X, 0, cameraLook.Z)

				if flatLook.Magnitude > 0 then
					lookDirection = flatLook.Unit
				end
			end

			currentGhostRoot.CFrame = CFrame.new(newPosition, newPosition + lookDirection)
		end

		currentGhostRoot.AssemblyLinearVelocity = Vector3.zero
		currentGhostRoot.AssemblyAngularVelocity = Vector3.zero
	end)
end

local function disableGhostMode()
	if not ghostModeEnabled then
		return
	end

	ghostModeEnabled = false

	if ghostConnection then
		ghostConnection:Disconnect()
		ghostConnection = nil
	end

	if ghostNoclipConnection then
		ghostNoclipConnection:Disconnect()
		ghostNoclipConnection = nil
	end

	if ghostRealKeepConnection then
		ghostRealKeepConnection:Disconnect()
		ghostRealKeepConnection = nil
	end

	destroyGhostMobileButtons()
	restoreRealCharacterToGhostPosition()

	if ghostClone then
		ghostClone:Destroy()
		ghostClone = nil
	end

	if Workspace.CurrentCamera and ghostOriginalCameraSubject and ghostOriginalCameraSubject.Parent then
		Workspace.CurrentCamera.CameraSubject = ghostOriginalCameraSubject
	elseif Workspace.CurrentCamera and ghostRealHumanoid then
		Workspace.CurrentCamera.CameraSubject = ghostRealHumanoid
	end

	ghostRealCharacter = nil
	ghostRealRoot = nil
	ghostRealHumanoid = nil
	ghostOriginalCameraSubject = nil
	ghostOriginalCFrame = nil
	ghostSafeCFrame = nil
	ghostSavedPartStates = {}
	ghostSavedHumanoidState = nil
end

--// UI TOGGLES

Visuals:CreateToggle({
	Name = "Highlight Survivors",
	Default = false,
	Callback = function(enabled)
		highlightSurvivorsEnabled = enabled

		if enabled then
			updatePlayerHighlights()
		else
			clearSurvivorHighlights()
		end
	end
})

Visuals:CreateToggle({
	Name = "Highlight Beast",
	Default = false,
	Callback = function(enabled)
		highlightBeastEnabled = enabled

		if enabled then
			updatePlayerHighlights()
		else
			clearBeastHighlights()
		end
	end
})

Visuals:CreateToggle({
	Name = "Highlight Computers",
	Default = false,
	Callback = function(enabled)
		if enabled then
			enableComputerHighlights()
		else
			disableComputerHighlights()
		end
	end
})

Movement:CreateToggle({
	Name = "Infinite Wall Hop",
	Default = false,
	Callback = function(enabled)
		if enabled then
			enableInfiniteWallHop()
		else
			disableInfiniteWallHop()
		end
	end
})

Movement:CreateToggle({
	Name = "Ghost Mode",
	Default = false,
	Callback = function(enabled)
		if enabled then
			enableGhostMode()
		else
			disableGhostMode()
		end
	end
})

--// PLAYER WATCHERS

for _, player in ipairs(Players:GetPlayers()) do
	watchPlayer(player)
end

Players.PlayerAdded:Connect(function(player)
	watchPlayer(player)
end)

Players.PlayerRemoving:Connect(function(player)
	removePlayerHighlight(player, "Survivor")
	removePlayerHighlight(player, "Beast")

	if characterConnections[player] then
		for _, connection in ipairs(characterConnections[player]) do
			connection:Disconnect()
		end

		characterConnections[player] = nil
	end
end)

LocalPlayer.CharacterAdded:Connect(function()
	lastWallHopJump = 0

	if ghostModeEnabled then
		disableGhostMode()
	end
end)

--// REFRESH LOOP FOR PLAYER HIGHLIGHTS

RunService.RenderStepped:Connect(function()
	if highlightSurvivorsEnabled or highlightBeastEnabled then
		updatePlayerHighlights()
	end
end)
